[package]
name = "cat-bot"
version = "0.1.0"
edition = "2024"
repository = "https://github.com/Ang2Tea/cat-bot"
readme = "README.md"

[dependencies]
tracing = { workspace = true }
thiserror ={ workspace = true }
serde = { version = "1.0.228", features = ["derive"] }
cat-bot-entities ={ path = "../cat-bot-entities" }
